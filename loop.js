for (let alertCount = 0; alertCount < 5; alertCount++) {
    // Runs 5 times, with values of step 0 through 4.
    //  if (alertCount < alertCutoff) {
        // alertCount += 1;
        alert("This is alert #" + alertCount);
    // }
    console.log('Loop test');
}